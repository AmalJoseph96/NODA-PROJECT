<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <title>Reset Password</title>
    <style>
        .form-group {
            position: relative;
            margin-bottom: 1rem;
        }

        .form-control {
            padding-right: 3rem; /* Increased space for the eye icon */
        }

        .toggle-password {
            position: absolute;
            top: 75%;
            right: 1rem;
            transform: translateY(-50%);
            cursor: pointer;
            pointer-events: auto; /* Ensure the eye icon is clickable */
            z-index: 1; /* Ensure the eye icon is above the input text */
        }
    </style>
</head>
<body class="d-flex justify-content-center align-items-center vh-100">
    <div class="card text-center" style="width: 400px;">
        <div class="card-header h5 text-white bg-success">Reset Password</div>
        <div class="card-body px-5">
            <form action="/verifyPassword" method="post">
                <p class="card-text py-2">
                    Enter your new password.
                </p>
                <div class="form-group">
                    <label for="newPassword">New Password</label>
                    <input type="password" id="newPassword" name="newPassword" class="form-control my-3" placeholder="Enter new password" required>
                    <span class="toggle-password" onclick="togglePasswordVisibility('newPassword')">👁️</span>
                </div>
                <div class="form-group">
                    <label for="confirmNewPassword">Confirm New Password</label>
                    <input type="password" id="confirmNewPassword" name="confirmNewPassword" class="form-control my-3" placeholder="Confirm new password" required>
                    <span class="toggle-password" onclick="togglePasswordVisibility('confirmNewPassword')">👁️</span>
                </div>
                <input type="submit" value="Reset Password" class="btn btn-success w-50">
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <% if (locals.message) { %>
        <script>
            Swal.fire({
                icon: 'success',
                title: 'Success',
                text: '<%= message %>'
            });
        </script>
    <% } %>

    <script>
        function togglePasswordVisibility(id) {
            const field = document.getElementById(id);
            const type = field.getAttribute('type') === 'password' ? 'text' : 'password';
            field.setAttribute('type', type);
        }
    </script>
</body>
</html>
